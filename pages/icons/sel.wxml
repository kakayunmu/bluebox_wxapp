<view class='page'>
  <view class='page_bd'>
    <view class='icon_items'>
      <view class='icon_item' wx:for="{{icons}}" style="height:{{item_height}}">
        <view class='content'>
          <i class='iconfont {{item.id}}'></i>
          <view> {{item.name}}</view>
        </view>
      </view>
    </view>
  </view>
</view>